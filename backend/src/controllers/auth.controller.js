const userModel = require('../models/user.model')


/**
 * @job Controller for registerUser
 * @params req {username, email, password}
 * @access Public
 */
async function registerUserController(req, res){

}

module.exports = registerUserController